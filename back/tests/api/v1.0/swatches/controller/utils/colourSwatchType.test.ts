// import { getAngle } from '../../../../../../src/api/v1.0/swatches/controller/utils/colourSwatchType'

// describe('Colour swatch Type', () => {

//   describe('getAngle', () => {
//     it('should return the angle for a primary swatch using a compound colour model', () => {
//       const angle = getAngle('compound', 'primary');
//       expect(angle).toBe(0);
//     });
//     it('should return the angle for a secondary swatch using a compound colour model', () => {
//       const angle = getAngle('compound', 'secondary');
//       expect(angle).toBe(210);
//     });
//     it('should return the angle for a primary swatch using a compound colour model', () => {
//       const angle = getAngle('compound', 'accent');
//       expect(angle).toBe(150);
//     });
//   })
// })